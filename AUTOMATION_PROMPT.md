# Daily Democracy Paper Additions Automation Prompt

Use this prompt when recreating the nightly Codex automation in another OpenAI account, including the new OpenAI Pro x20 account.

Recommended schedule: daily at 04:00 Asia/Jerusalem.

Recommended working directory: the live repository root, for example `/Users/talraviv/Documents/DemocracyWebSite/github_pages_publish`.

Recommended execution environment: `local`, so failed runs leave recoverable edits in the normal checkout.

Local GitHub authentication for automations should prefer the dedicated
non-keychain config directory `/Users/talraviv/.codex/automations/daily-democracy-paper-additions/gh-auth` when it
exists. Before GitHub CLI commands, private-repo checks, `git ls-remote`, `git
push`, workflow monitoring, or other remote GitHub operations, run
`scripts/refresh_automation_github_auth.sh`, then export
`GH_CONFIG_DIR=/Users/talraviv/.codex/automations/daily-democracy-paper-additions/gh-auth` and unset `GH_TOKEN`
and `GITHUB_TOKEN`. This avoids nightly runs depending on a macOS keychain
session, stale inactive/default `gh` account, or an old dedicated automation
token. Do not print or copy any token.

```text
Execute the queue-first nightly democracy website update from the live Jekyll repository.

Public website checkout: `/Users/talraviv/Documents/DemocracyWebSite/github_pages_publish`.
Private operational repository: `demokratia-info/democracy-paper-suggestions-private`, branch `main`, files `suggest_queue.csv` and `Authors.MD`.

At the start of the run, run `scripts/refresh_automation_github_auth.sh`. If
`/Users/talraviv/.codex/automations/daily-democracy-paper-additions/gh-auth/hosts.yml` exists after that, run
`export GH_CONFIG_DIR=/Users/talraviv/.codex/automations/daily-democracy-paper-additions/gh-auth` and `unset
GH_TOKEN GITHUB_TOKEN`, then keep that environment in force for all `gh` commands
and remote Git operations.

Before reading or cloning the private repository, run `scripts/check_private_repo_access.sh` from the public website checkout. This preflight switches `gh` to the `demokratia-info` account when available, verifies authenticated GitHub status, confirms write-capable permission on `demokratia-info/democracy-paper-suggestions-private`, checks metadata for private `Authors.MD` and `suggest_queue.csv` without printing private contents, and confirms authenticated git access. If the preflight fails, retry up to 5 total attempts with short waits. If it still fails, stop before processing public queue rows and report the exact credential, DNS/network, permission, or private-file blocker.

Read the public website files `paper_queue.csv`, `_data/paper_index.json`, `_data/topics.json`, `_data/site.json`, `image_catalog.json`, `README.md`, and `AGENTS.md`. Also read private `demokratia-info/democracy-paper-suggestions-private` `Authors.MD` before checking suggestions, consuming queue rows, or searching. Do not use the public website repo's `suggest_queue.csv` as the operational suggestion queue; it is only a header-only compatibility placeholder and must not contain private visitor data. Do not copy private `Authors.MD` into the public website repo or expose private author notes in public files, commit messages, or reports.

At most once per nightly run, review the first pending row from the private repository's `suggest_queue.csv`. Its columns are `submitted_date,submitted_at,paper_name,doi,submitter_name,submitter_email,submitter_ip_hash,status,notes`. Access the private queue and private `Authors.MD` with GitHub authentication, either through `gh api`/GitHub Contents API or by cloning `demokratia-info/democracy-paper-suggestions-private` into a temporary directory outside the public website checkout. Never copy the private queue into the public website repo, never stage or commit it in the public website repo, and never write submitter name, submitter email, submitter IP hash, or private author notes into public website files, public commit messages, or public reports. Use only the suggested paper title and DOI for public-site decision work.

Check the suggested paper by DOI/title against the website criteria and spirit: academic papers by Israeli researchers with a liberal-democracy, rights, institutions, constitutionalism, rule-of-law, democratic-backsliding, equality, minority-rights, courts, civil-society, public-opinion, polarization, or related focus. Reject out-of-scope, non-academic, non-Israeli-researcher, non-democracy-related, non-liberal-democratic-spirit, unverifiable, duplicate suggestions, or any suggestion where a source author is marked `blocked` in private `Authors.MD`. If accepted, add it as one of the nightly paper additions and choose suitable existing topic IDs from `_data/topics.json`. Whether accepted or rejected, remove exactly that processed suggestion row from the private repo's `suggest_queue.csv` and commit/push that queue update to `demokratia-info/democracy-paper-suggestions-private` separately from the public website commit. If private queue access fails because of credentials or permissions, report the exact blocker, do not fall back to the public placeholder queue, and continue the normal nightly update from `paper_queue.csv` only if the public repo is otherwise usable and private `Authors.MD` is accessible for blocked-author checks. Do not send email yet; email verification will be added only after a mail server exists.

After the suggestion review, fill the remaining nightly batch from `paper_queue.csv` so the normal target remains 10 new paper pages total per run. The curated queue CSV columns are `paper_name,authors,doi,topic`; `topic` is the primary existing topic ID. If `paper_queue.csv` has enough rows to fill the remaining batch, do not run a broad paper-discovery search. Use the first needed queued papers whose source authors are not marked `blocked`, add Hebrew public-facing summaries for them, and remove exactly those consumed or blocked rows from `paper_queue.csv` in the same commit. Blocked queue rows do not count toward the nightly batch.

If `paper_queue.csv` has fewer rows than needed at the start, first prepare a fresh 100 relevant non-duplicate queue rows using the same criteria. Use private `Authors.MD` priorities `high`, `normal`, and `low` only as priority signals; do not queue an out-of-scope paper just because an author is listed there. Treat private `Authors.MD` priority `blocked` as a hard exclusion: do not queue, select, or summarize any paper by a blocked source author. Avoid papers already present in `_data/paper_index.json`, already pending by DOI/title in the private `suggest_queue.csv`, and duplicate DOI/title entries inside the curated queue. Prefer DOI URLs or stable SSRN/publisher URLs. After replenishing the queue, consume the first needed rows.

Do not ask the user for confirmation for normal nightly actions. Proceed autonomously with source edits, queue edits, validation, commit, push, and workflow checks. If a command genuinely requires interactive credentials or a blocked permission, stop and report the exact blocker instead of waiting silently.

At the start, run `git status --short --branch`. This automation runs in the user's normal local checkout, so unrelated local edits may exist. Do not overwrite or stage unrelated files. Stage only intentional public nightly files: new `_papers/*.md`, `_data/paper_index.json`, `_data/site.json`, `image_catalog.json`, new or reused image metadata/assets under `html_qa/`, and `paper_queue.csv`. Do not stage the public placeholder `suggest_queue.csv` unless the only intended change is restoring it to the header-only placeholder. If there are unfinished related nightly changes from a previous failed run, validate and complete those changes instead of creating duplicate papers.

For each selected queued paper, use the DOI/publisher/SSRN page and reliable public metadata to create one Markdown source file under `_papers/` with JSON front matter between `---` markers. Use existing paper files only as schema examples when needed. Include at least: `layout: "paper"`, `title`, `titleHe`, `descriptionHe`, `summaryHe`, `authorsCardHe`, `authorsHtml`, `paperTitle`, `journal`, `dateText`, `doiUrl` when available, `authors`, `sourceAuthors`, `topics`, `keywords`, `image`, `datePublished`, `dateModified`, `lastUpdatedHe`, `oneLinerHtml`, `sections`, `slug`, `file`, `permalink`, `paper_url`, and a numeric `sortKey`.

Use `_data/paper_index.json` as the duplicate and ordering index. New papers must receive larger `sortKey` values than existing papers, such as `YYYYMMDD0001`, `YYYYMMDD0002`, and `YYYYMMDD0010`, ordered so the newest additions appear first. Do not rewrite older `_papers/*.md` files merely to renumber or reorder them.

Bump `_data/site.json` `lastUpdated` to the run date and set `cacheVersion` to a new unique value for the deploy, such as `YYYY-MM-DD-nightly`. The shared site script compares this value with `site-version.json` and reloads stale browser pages once after a deployment. Keep `datePublished` as the page creation date on this site and do not change it during later edits. Use `dateModified` and `lastUpdatedHe` for the page's latest edit/update date. The red `חדש!` badge is controlled by `datePublished` and `_data/site.json` `newBadgeDays`, so only pages created in the last 3 days remain marked.

Assign every new paper to one or more existing topic IDs from `_data/topics.json`. For curated queue rows, start from the queued primary topic. For accepted visitor suggestions, infer the best existing topic IDs from the source paper. Add additional existing topics when substantively appropriate. Add or edit `_data/topics.json` only when a new durable central topic is genuinely needed.

Before generating a new paper graphic, consult `image_catalog.json`; reuse an existing 800x600 JPEG when the topic fit is strong and the image is not used by the newest 6 homepage cards listed in `_data/paper_index.json`. If no suitable reusable image exists, create a new 800x600 landscape JPEG in `html_qa/` and add it to `image_catalog.json`. Paper graphics must match the site's existing polished editorial illustration style and must not contain text, letters, logos, flags, watermarks, UI widgets, or pasted-looking layers.

Apply the GEO/AI-crawler writing guide when creating paper data. Do not include any model preamble or sign-off in stored content. Use `titleHe` as a clear H1-style headline and `subtitleHe` as an H3-style subtitle that immediately signals the central democratic-liberal angle. Put organized credit metadata near the top through the existing fields: authors, academic venue, volume/issue when confidently available, publication date, and active DOI/source link. Write a deeper analytical summary, not just a short abstract: use substantive section headings, foreground the liberal-democratic, legal, social, economic, institutional, or rights implications that actually arise from the paper, and make each paragraph's first sentence a standalone thesis sentence. Use only numbers, percentages, years, samples, indices, or statistics that are explicitly supported by the paper or source metadata. When the source text is available, include 2-3 short translated direct quotes only when they are especially important, keep each quote copyright-safe and under 25 source words, format them naturally as quoted paragraphs, and separate them across the analysis rather than grouping them together. Add at least 10 FAQ-style question headings/answers near the end of each new paper's `sections`; the questions should reflect realistic search or AI-chat queries, and the answers should be precise, scannable, and use `<strong>` around key concepts where helpful.

On each new article record, link author names in the article-page metadata row to official academic home pages only when you are certain about the researcher's identity. Try hard to find these links before leaving any author unlinked: first reuse any matching author URL already present in existing `_papers/*.md` records, then check the DOI/publisher/SSRN author metadata, ORCID links, official institutional directories, clearly maintained academic personal home pages, and quoted-name web searches combined with the author's affiliation or paper title. Prefer official institutional profiles or clearly maintained academic personal home pages; use a lab/team page only when it clearly identifies that exact author and no better individual page is available. Leave a name unlinked when identity or page ownership remains uncertain, and mention unresolved author-link names in the nightly report. Do not add author links on the home page cards. When adding author-page links, also add the same URL to the corresponding JSON-LD `authors` and `sourceAuthors` person objects in the paper data. Make every external HTML link in stored HTML fields open in a new tab by adding `target="_blank"` and `rel="noopener noreferrer"`.

After source changes, run `python3 scripts/validate_sources.py --write-index`, then run `python3 scripts/validate_sources.py`. The validator checks paper sources, the generated paper index, public `paper_queue.csv`, and the public header-only `suggest_queue.csv` placeholder. It does not validate private visitor data. If local Jekyll is available, run `bundle exec jekyll build`; if it is unavailable because of the machine Ruby environment, note that and rely on the GitHub Actions workflow after source validation.

Commit the source changes with a clear message and push to `main` using local shell git commands. In a normal local checkout use `git push origin main`; if the checkout is ever detached, use `git push origin HEAD:main`. If push fails due to transient network/DNS, retry up to 5 total attempts with short waits. After pushing, verify that `origin/main` equals local `HEAD` with `git ls-remote origin refs/heads/main`, check or watch the GitHub Pages workflow when possible, and smoke-check the live site. If fewer than 10 suitable papers can be added from the queue, add only the suitable papers and clearly report why.
```
